# 雨避けVRゲーム 仕様書

## 0. 前提

本仕様書は「開発計画書」（技術スタック・ディレクトリ構成・ブランチ運用）を前提とし、
**ゲーム内容そのもの（何が起き、どう遊べるか）**を定義するものです。

---

## 1. プロダクト概要

PICO 4 Ultra上で動作するWebXR製の雨避けMR（複合現実）ゲーム。
プレイヤーは**現実世界の風景（パススルー）**の中に降る、ゆっくりとした雨を避け続ける。
クリア後、自分のプレイをリプレイ映像として振り返ると、実は自分が**現実の雨速（約6〜9 m/s）で降る雨をリアルタイムで避けていた**ことに気づく——という「体感のギャップ」を核とした体験。

---

## 2. コアコンセプト

- ゲーム中はプレイヤーにとって雨が「ゆっくり」に見える（実際は `RAIN_SPEED_SLOW` で描画）
- クリア後のリプレイでは、同じプレイの記録データを**プレイヤーの動きと雨の両方を同時に** `REPLAY_MULTIPLIER` 倍速で再生する
- 倍速後の雨速は現実の雨速（`RAIN_SPEED_REAL`）と一致するよう設計されており、「自分は本物の雨をよけていたんだ」という驚きを生む
- **重要**：雨だけ、あるいはプレイヤーだけを倍速にすることはしない。両方を同じ倍率で同時に倍速再生し、ゲーム中と同じ相対関係（雨とプレイヤーの位置関係）を保つ

---

## 3. ゲームループ

### 3.1 状態遷移

```
START → PLAYING → (CLEAR または GAMEOVER) → REPLAY → START（再挑戦）
```

| 状態 | 内容 |
|---|---|
| START | スタート画面。プレイヤーの準備ができたら開始 |
| PLAYING | 雨が降り、避け続ける。制限時間 or ライフ切れで終了 |
| CLEAR | 制限時間（初期値30秒、後で調整可能な変数として実装）を生き残った |
| GAMEOVER | ライフが0になった |
| REPLAY | 直前のプレイを記録データから三人称で再生。終了後はSTARTに戻り、即再挑戦できる |

### 3.2 クリア条件・ライフ

- 制限時間：初期値 **30秒**（`GAME_DURATION` として定数化し、後で調整しやすくする）
- ライフ：**3回**（雨に当たるごとに1減少、0でGAMEOVER）
- ライフが尽きてもクリアしても、必ずREPLAYに遷移する（当たった/避けた記録を両方見せる）

---

## 4. 雨システム

### 4.1 降り方

- 降る位置・タイミングは**ランダム**（固定パターンなし）
- 基本の降下方向は**真上から垂直**
- 斜め方向の雨は**難易度パラメータとして将来追加**（初期実装では真上のみでOK）
- 同時に存在する雨粒数：`RAIN_COUNT = 150`（まずこの値で難易度を試し、要調整）
- 初期の難易度方針：**「よけられる」ことを最優先**。密度・速度は避けゲーとして成立するかを実機テストしながら調整する前提

### 4.2 難易度の可変性

- `RAIN_SPEED_SLOW`、`RAIN_COUNT`、`GAME_DURATION` は全て `constants.js` で一元管理し、後から数値調整のみで難易度を変えられるようにする
- 時間経過による難易度変化（雨が増える等）は**初期スコープ外**。将来拡張候補

---

## 5. 当たり判定

- 判定対象：**頭 + 左右コントローラー（手）**（`PlayerCollider.js` にて球コライダー）
  - 頭の半径：`PLAYER_HEAD_RADIUS = 0.15m`
  - 手のコライダー半径は別途定義（開発計画書には未記載のため実装時に決定）
- 傘・防御アイテムなどのインタラクションは**初期実装では実装しない**（将来拡張として設計上は余地を残す）
- 回避手段：**移動**（ルームスケールでのしゃがみ・ステップ・体をずらす動作）が基本

---

## 6. リプレイ機能（本ゲームの核）

### 6.1 記録データ

`Recorder.js` が毎フレーム、以下を記録する：

- 頭の位置・回転（HMDのポーズ）
- 左右コントローラーの位置・回転
- 各雨粒の位置（`RainPhysics.positions`）
- 被弾イベント（いつ・どこで当たったか）

このデータをもとに `Replayer.js` が `REPLAY_MULTIPLIER` 倍速で**プレイヤーの動きと雨の両方を同時に**再生する。

### 6.2 視点・アバター

- **三人称視点**で再生する
- プレイヤー自身の見た目：**人型の簡易アバター（棒人間〜低ポリ）**を新規に用意し、記録した頭・手の位置に合わせて動かす
- 雨は記録された位置データをそのまま倍速再生でトレースする

### 6.3 リプレイカメラ操作

- **フリーカメラ方式**：リプレイ中、プレイヤーはコントローラー操作でカメラの**位置を移動**させながら、**視点の向きも自由に**動かせる（位置固定の定点観測ではない）
- 想定操作：スティックで前後左右移動、コントローラーの向きで視点回転（VR内フリーカメラの一般的な操作感）
- 実装上の注意点（未決事項・要検討）：
  - 移動範囲の制限（プレイエリア外に出た場合の処理）
  - 移動時の酔い対策（スナップターン／フェード等の検討が必要）
  - 巻き戻し・再生速度変更・一時停止などの操作は初期スコープに含めるか要判断（未定。含めない場合は「最初から最後まで自動再生を見るだけ」でよい）

### 6.4 背景（MRパススルー）

- 背景は**PICO 4 Ultraのパススルー機能を使い、リアルタイムの実際の周囲（現実世界）を透過表示**
- 雨（およびリプレイ時のアバター）のみをCG（Three.js）でオーバーレイ描画する
- **技術的な確認事項（要Phase 1での検証）**：
  - WebXR Device APIでPICO 4 UltraのMR/パススルーモード（`immersive-ar` セッション、または該当するWebXR機能）が利用可能か
  - パススルー使用時のパフォーマンス（雨粒150個のInstancedMesh描画と両立できるか）

---

## 7. 演出方向性

- 全体トーンは**リアル寄り**（ファンタジー的な過剰演出は避ける）
- 背景は現実世界そのもの（パススルー）のため、演出として追加するのは基本的に**雨のみ**
- 雨の見た目（水滴の質感・光の反射など）で「本物の雨らしさ」を出す方向

---

## 8. スコープ（短期開発）

### 8.1 必須（MVP）

- [ ] 雨をランダムに降らせ、頭・手の当たり判定で避けられる
- [ ] 3ライフ制、30秒生存でクリア／被弾3回でゲームオーバー
- [ ] プレイ内容（頭・手・雨の位置）を記録
- [ ] リプレイを三人称・人型簡易アバター・フリーカメラで再生
- [ ] MRパススルー背景 + 雨のCGオーバーレイ
- [ ] クリア/ゲームオーバー後、リプレイ→再挑戦の一連フロー

### 8.2 初期スコープ外（将来拡張の余地を残す）

- 傘などの防御アイテム・追加インタラクション
- 斜め雨・時間経過による難易度上昇
- リプレイの巻き戻し・一時停止・速度変更UI
- リプレイカメラの移動範囲制限や酔い対策の作り込み

---

## 9. 定数一覧（初期値・開発計画書より継承）

```js
export const RAIN_SPEED_SLOW = 1.5;    // m/s（ゲーム中の見かけの雨速）
export const RAIN_SPEED_REAL = 7.0;    // m/s（現実の雨速・リプレイ後の速度）
export const REPLAY_MULTIPLIER = RAIN_SPEED_REAL / RAIN_SPEED_SLOW;
export const RAIN_COUNT = 150;
export const PLAYER_HEAD_RADIUS = 0.15; // m
export const GAME_DURATION = 30;        // 秒（調整可能パラメータとして扱う）
export const PLAYER_LIVES = 3;          // 被弾許容回数
```

---

## 10. 未決事項リスト（次に詰めるべきこと）

1. WebXRでPICO 4 UltraのパススルーMRモードを扱う具体的な実装方法・API確認
2. リプレイのフリーカメラの移動範囲・酔い対策・移動限界の処理
3. コントローラー（手）側の当たり判定コライダーの半径
4. リプレイ中の追加操作（一時停止・巻き戻し・速度変更）を実装するか否か
5. サウンド（雨音・被弾音・クリア音等）の方向性
6. HUD（タイマー・ライフ表示）の具体的な見た目・配置
