# 雨避けVRゲーム 仕様書

## 0. 前提

本仕様書は「開発計画書」（技術スタック・ディレクトリ構成・ブランチ運用）を前提とし、
**ゲーム内容そのもの（何が起き、どう遊べるか）**を定義するものです。

---

## 1. プロダクト概要

PICO 4 Ultra上で動作するWebXR製の雨避けMR（複合現実）ゲーム。
プレイヤーは**現実世界の風景（パススルー）**の中に降る、ゆっくりとした雨を避け続ける。
クリア後、自分のプレイをリプレイとして振り返ると、まるで**現実の雨速（約6〜9 m/s）で降る雨を避けていたように見える**——という「体感のギャップ」を核とした体験。

---

## 2. コアコンセプト

- ゲーム中はプレイヤーにとって雨が「ゆっくり」に見える（実際は `RAIN_SPEED_SLOW` で描画）
- クリア後のリプレイでは、同じプレイの記録データを**プレイヤーの動きと雨の両方を同時に** `REPLAY_MULTIPLIER` 倍速で再生する
- 倍速後の雨速は現実の雨速（`RAIN_SPEED_REAL`）と一致するよう設計されており、「まるで本物の雨をよけていたかのように見える」という驚きを生む
- **重要**：雨だけ、あるいはプレイヤーだけを倍速にすることはしない。両方を同じ倍率で同時に倍速再生し、ゲーム中と同じ相対関係（雨とプレイヤーの位置関係）を保つ

---

## 3. ゲームループ

### 3.1 状態遷移

```
START → PLAYING → (CLEAR または GAMEOVER) → REPLAY → START（自動遷移・再挑戦）
```

| 状態 | 内容 |
|---|---|
| START | スタート画面。トリガーボタンで開始 |
| PLAYING | 雨が降り、避け続ける。制限時間 or ライフ切れで終了 |
| CLEAR | 制限時間（初期値30秒）を生き残った |
| GAMEOVER | ライフが0になった |
| REPLAY | 直前のプレイを記録データから再生。終了後は**自動でSTARTに戻る** |

- CLEAR・GAMEOVERどちらも同じ演出でREPLAYに遷移する（演出の差異なし）
- リプレイ中の一時停止・巻き戻しは**初期実装では行わない**（将来拡張候補）

### 3.2 クリア条件・ライフ

- 制限時間：初期値 **30秒**（`GAME_DURATION` として定数化）
- ライフ：**3回**（雨に当たるごとに1減少、0でGAMEOVER）
- ライフが尽きてもクリアしても、必ずREPLAYに遷移する

---

## 4. 雨システム

### 4.1 降り方

- 降る位置・タイミングは**ランダム**（固定パターンなし）
- 基本の降下方向は**真上から垂直**
- 斜め方向の雨は**難易度パラメータとして将来追加**（初期実装では真上のみでOK）
- 同時に存在する雨粒数：`RAIN_COUNT = 150`（まずこの値で難易度を試し、要調整）
- 初期の難易度方針：**「よけられる」ことを最優先**。密度・速度は実機テストしながら調整する前提

### 4.2 難易度の可変性

- `RAIN_SPEED_SLOW`、`RAIN_COUNT`、`GAME_DURATION` は全て `constants.js` で一元管理し、後から数値調整のみで難易度を変えられるようにする
- 時間経過による難易度変化（雨が増える等）は**初期スコープ外**。将来拡張候補

---

## 5. 当たり判定

- 判定対象：**頭 + 左右コントローラー（手）**（`PlayerCollider.js` にて球コライダー）
  - 頭の半径：`PLAYER_HEAD_RADIUS = 0.15m`
  - 手の半径：`PLAYER_HAND_RADIUS = 0.15m`（頭と同じ。実機調整前提）
- 傘・防御アイテムなどのインタラクションは**初期実装では実装しない**
- 回避手段：**移動**（ルームスケールでのしゃがみ・ステップ・体をずらす動作）が基本

### 被弾時の演出（3つ同時）

1. **画面フラッシュ**（赤く瞬間点灯）
2. **被弾SE**（フリー素材）
3. **コントローラー振動**（WebXR Haptic Actuator）

---

## 6. リプレイ機能（本ゲームの核）

### 6.1 記録データ

`Recorder.js` が毎フレーム、以下を記録する：

- 頭の位置・回転（HMDのポーズ、`local-floor` 参照空間）
- 左右コントローラーの位置・回転
- 各雨粒の位置（`RainPhysics.positions`）
- 被弾イベント（いつ・どこで当たったか）

このデータをもとに `Replayer.js` が `REPLAY_MULTIPLIER` 倍速で**プレイヤーの動きと雨の両方を同時に**再生する。

### 6.2 視点・アバター

- リプレイはARのまま継続（パススルーを維持）
- プレイヤーは**物理的に歩き回って**好きな角度から見る（バーチャルカメラ移動なし）
- アバターの見た目：**棒人間（スティックマン）**（頭・手を球、胴体を棒で近似）
- アバターと雨は `local-floor` 座標でゲーム中と同じ位置に表示されるため、部屋の描画は不要
- 雨は記録された位置データをそのまま倍速再生でトレースする

> **技術的根拠**：WebXRの `local-floor` 参照空間は同一セッション内で原点が固定されるため、ゲーム中に記録した座標をリプレイでそのまま使い回せる。セッションを切らないことが条件。

### 6.3 背景（MRパススルー）

- 背景は**PICO 4 Ultraのパススルー機能を使い、現実世界を透過表示**
- 雨・アバターのみをCG（Three.js）でオーバーレイ描画する
- **フォールバック**：パススルー（`immersive-ar`）が動作しない場合は `immersive-vr` に切り替え、**宇宙空間（星空背景）**を表示する
- パフォーマンス確認事項（Phase 1で検証）：パススルー使用時に雨粒150個のInstancedMesh描画と両立できるか

---

## 7. UI / HUD

- HUDは**視界に固定追従**（頭の動きに追従して常に視界内に表示）
- タイマー：残り秒数を数字で表示
- ライフ：**ハートアイコン3つ**（被弾で1つずつ消える）
- **リプレイ中も同じHUDを表示する**：記録データから復元した「その瞬間の残り秒数・残りライフ」を表示し、被弾した瞬間にハートが減る様子・タイマーが進んでいく様子をリプレイでも確認できるようにする

---

## 8. サウンド

- 素材：**フリー素材**を使用
- 最低限必要なSE：被弾音・クリア音・雨音（ループ）
- BGMは初期スコープ外

---

## 9. 演出方向性

- 全体トーンは**リアル寄り**（ファンタジー的な過剰演出は避ける）
- 背景は現実世界そのもの（パススルー）のため、演出として追加するのは基本的に**雨のみ**
- 雨の見た目（水滴の質感・光の反射など）で「本物の雨らしさ」を出す方向

---

## 10. スコープ（短期開発）

### 10.1 必須（MVP）

- [ ] 雨をランダムに降らせ、頭・手の当たり判定で避けられる
- [ ] 3ライフ制、30秒生存でクリア／被弾3回でゲームオーバー
- [ ] 被弾時：画面フラッシュ + SE + コントローラー振動
- [ ] プレイ内容（頭・手・雨の位置）を記録
- [ ] リプレイをARのまま棒人間アバターで再生（プレイヤーが物理的に歩き回って観察）
- [ ] リプレイ終了後、自動でSTARTに戻る
- [ ] MRパススルー背景 + 雨のCGオーバーレイ
- [ ] HUD：視界追従、ハートアイコン・タイマー表示

### 10.2 初期スコープ外（将来拡張の余地を残す）

- 傘などの防御アイテム・追加インタラクション
- 斜め雨・時間経過による難易度上昇
- リプレイの一時停止・巻き戻し・速度変更UI
- BGM

---

## 11. 定数一覧

```js
export const RAIN_SPEED_SLOW = 1.5;     // m/s（ゲーム中の見かけの雨速）
export const RAIN_SPEED_REAL = 7.0;     // m/s（現実の雨速・リプレイ後の速度）
export const REPLAY_MULTIPLIER = RAIN_SPEED_REAL / RAIN_SPEED_SLOW; // ≒ 4.67倍
export const RAIN_COUNT = 150;          // 同時に存在する雨粒数
export const PLAYER_HEAD_RADIUS = 0.15; // m
export const PLAYER_HAND_RADIUS = 0.15; // m（実機調整前提）
export const GAME_DURATION = 30;        // 秒
export const PLAYER_LIVES = 3;          // 被弾許容回数
```
